# Finding schema

Each finding includes `finding_id`, `title`, `description`, `severity`, `category`, `evidence`, `affected_asset`, `risk`, `recommendation`, `references`, and `compliance_mappings`. Risk inputs are also retained: exploitability, asset criticality, exposure, and confidence.

Severity values are `CRITICAL`, `HIGH`, `MEDIUM`, `LOW`, and `INFO`.
